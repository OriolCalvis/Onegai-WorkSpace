{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Motor · juego (macOS Debug)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder:Motor}/out/build/macos-debug/juego",
            "args": [],
            "cwd": "${workspaceFolder:Motor}",
            "MIMode": "lldb",
            "preLaunchTask": "CMake · Motor: compilar juego (Debug)"
        }
    ]
}
